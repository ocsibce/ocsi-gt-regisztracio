import React from 'react';

const Form : React.FC = props => {
    return (
        <form>
            <input type="text" name="nev" id="nev" placeholder="nev"/>
        </form>
    );
}

export default Form;