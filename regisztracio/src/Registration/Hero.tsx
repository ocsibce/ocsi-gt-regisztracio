import React from 'react';
import styled from 'styled-components';

const Hero : React.FC = props => {
    return (
        <header>
            <h1>Közgáz Gólyatábor 2020</h1>
        </header>
    );
}

export default Hero;